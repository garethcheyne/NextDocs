# Development Dockerfile with hot reload
FROM node:20-alpine

WORKDIR /app

# Install dependencies for node-gyp
RUN apk add --no-cache python3 make g++

# Copy package files
COPY package.json package-lock.json* ./

# Install dependencies
RUN npm install

# Copy Prisma schema
COPY prisma ./prisma

# Generate Prisma Client
RUN npx prisma generate

# Copy the entire project for development
COPY . .

# Expose development port
EXPOSE 9980

# Set environment variables
ENV PORT=9980
ENV HOSTNAME="0.0.0.0"
ENV NEXT_TELEMETRY_DISABLED=1

# Ensure scripts directory exists and init script is executable
RUN mkdir -p /app/scripts && \
    if [ -f /app/scripts/init-worker.sh ]; then chmod +x /app/scripts/init-worker.sh; fi

# Start development server with hot reload and worker in background
CMD sh -c "npm run dev & sleep 10 && if [ -f /app/scripts/init-worker.sh ]; then /app/scripts/init-worker.sh; fi && wait"
